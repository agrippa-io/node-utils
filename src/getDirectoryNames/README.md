# getDirectoryNames

Returns the names of all immediate subdirectories of a path.

```ts
import { getDirectoryNames } from '@agrippa-io/node-utils'

const dirs = getDirectoryNames('/path/to/modules')
```
